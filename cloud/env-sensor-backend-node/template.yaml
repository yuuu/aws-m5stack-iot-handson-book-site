AWSTemplateFormatVersion: 2010-09-09
Description: >-
  env-sensor-backend-node

Transform:
- AWS::Serverless-2016-10-31

Globals:
  Function:
    Timeout: 10
    MemorySize: 512
    Runtime: nodejs22.x
    Architectures:
      - x86_64
    Environment:
      Variables:
        SENSOR_READINGS_TABLE: !Ref SensorReadingsTable
        TZ: Asia/Tokyo

Resources:
  DashboardFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/dashboard/
      Handler: app.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref SensorReadingsTable
      Events:
        Api:
          Type: Api
          Properties:
            Path: /
            Method: GET

  SensorReadingsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      AttributeDefinitions:
        - AttributeName: device_id
          AttributeType: S
        - AttributeName: timestamp
          AttributeType: N
      KeySchema:
        - AttributeName: device_id
          KeyType: HASH
        - AttributeName: timestamp
          KeyType: RANGE
      BillingMode: PAY_PER_REQUEST

  IoTTopicRuleDynamoDBv2:
    Type: AWS::IoT::TopicRule
    Properties:
      TopicRulePayload:
        Sql: SELECT *, topic(2) AS device_id, timestamp() AS timestamp FROM 'env-sensor/#'
        RuleDisabled: false
        Actions:
          - DynamoDBv2:
              PutItem:
                TableName: !Ref SensorReadingsTable
              RoleArn: !GetAtt IoTTopicRuleIAMRole.Arn

  IoTTopicRuleIAMRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - iot.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      Policies:
        - PolicyName: !Sub '${AWS::StackName}-IoTRuleDynamoDBPolicy'
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                Resource: !GetAtt SensorReadingsTable.Arn

Outputs:
  WebEndpoint:
    Description: "API Gateway endpoint URL for Prod stage"
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/"
